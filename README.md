A repository of some workshops I've given to friends and family

This uses `marp` to generate the presentations

### Installation
`brew install marp-cli`

### Building:
In the correct directory run:
`marp-cli presentation.md`
